name: Terraform Infrastructure

on:
  push:
    branches:
      - main
    paths:
      - 'envs/**'
      - 'modules/**'
  pull_request:
    branches:
      - main
    paths:
      - 'envs/**'
      - 'modules/**'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy'
        required: true
        type: choice
        options:
          - dev
          - qa
          - prod
      action:
        description: 'Terraform action'
        required: true
        type: choice
        options:
          - plan
          - apply
          - destroy

jobs:
  terraform-dev:
    runs-on: ubuntu-latest
    environment: dev
    if: |
      (github.ref == 'refs/heads/main' && github.event_name == 'push') ||
      github.event_name == 'pull_request' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'dev')

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.7.0

    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Terraform Init
      working-directory: ./envs/dev
      run: terraform init

    - name: Terraform Format Check
      working-directory: ./envs/dev
      run: terraform fmt -check
      continue-on-error: true

    - name: Terraform Validate
      working-directory: ./envs/dev
      run: terraform validate

    - name: Terraform Plan
      working-directory: ./envs/dev
      env:
        TF_VAR_postgres_admin_password: ${{ secrets.POSTGRES_ADMIN_PASSWORD }}
      run: terraform plan -out=tfplan

    - name: Terraform Apply
      if: |
        (github.ref == 'refs/heads/main' && github.event_name == 'push') ||
        (github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply')
      working-directory: ./envs/dev
      env:
        TF_VAR_postgres_admin_password: ${{ secrets.POSTGRES_ADMIN_PASSWORD }}
      run: terraform apply -auto-approve tfplan

    - name: Terraform Destroy
      if: github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'destroy'
      working-directory: ./envs/dev
      env:
        TF_VAR_postgres_admin_password: ${{ secrets.POSTGRES_ADMIN_PASSWORD }}
      run: terraform destroy -auto-approve

    - name: Output Terraform Values
      if: |
        (github.ref == 'refs/heads/main' && github.event_name == 'push') ||
        (github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply')
      working-directory: ./envs/dev
      run: terraform output -json > dev_outputs.json

    - name: Upload Terraform Outputs
      if: |
        (github.ref == 'refs/heads/main' && github.event_name == 'push') ||
        (github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply')
      uses: actions/upload-artifact@v4
      with:
        name: dev-terraform-outputs
        path: ./envs/dev/dev_outputs.json

  terraform-qa:
    needs: terraform-dev
    runs-on: ubuntu-latest
    environment: qa
    if: |
      (github.ref == 'refs/heads/main' && github.event_name == 'push') ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'qa')

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.7.0

    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Terraform Init
      working-directory: ./envs/qa
      run: terraform init

    - name: Terraform Format Check
      working-directory: ./envs/qa
      run: terraform fmt -check
      continue-on-error: true

    - name: Terraform Validate
      working-directory: ./envs/qa
      run: terraform validate

    - name: Terraform Plan
      working-directory: ./envs/qa
      env:
        TF_VAR_postgres_admin_password: ${{ secrets.POSTGRES_ADMIN_PASSWORD }}
      run: terraform plan -out=tfplan

    - name: Terraform Apply
      if: |
        (github.ref == 'refs/heads/main' && github.event_name == 'push') ||
        (github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply')
      working-directory: ./envs/qa
      env:
        TF_VAR_postgres_admin_password: ${{ secrets.POSTGRES_ADMIN_PASSWORD }}
      run: terraform apply -auto-approve tfplan

    - name: Terraform Destroy
      if: github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'destroy'
      working-directory: ./envs/qa
      env:
        TF_VAR_postgres_admin_password: ${{ secrets.POSTGRES_ADMIN_PASSWORD }}
      run: terraform destroy -auto-approve

    - name: Output Terraform Values
      if: |
        (github.ref == 'refs/heads/main' && github.event_name == 'push') ||
        (github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply')
      working-directory: ./envs/qa
      run: terraform output -json > qa_outputs.json

    - name: Upload Terraform Outputs
      if: |
        (github.ref == 'refs/heads/main' && github.event_name == 'push') ||
        (github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply')
      uses: actions/upload-artifact@v4
      with:
        name: qa-terraform-outputs
        path: ./envs/qa/qa_outputs.json

  terraform-prod:
    needs: terraform-qa
    runs-on: ubuntu-latest
    environment: prod
    if: |
      (github.ref == 'refs/heads/main' && github.event_name == 'push') ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'prod')

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.7.0

    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Terraform Init
      working-directory: ./envs/prod
      run: terraform init

    - name: Terraform Format Check
      working-directory: ./envs/prod
      run: terraform fmt -check
      continue-on-error: true

    - name: Terraform Validate
      working-directory: ./envs/prod
      run: terraform validate

    - name: Terraform Plan
      working-directory: ./envs/prod
      env:
        TF_VAR_postgres_admin_password: ${{ secrets.POSTGRES_ADMIN_PASSWORD }}
      run: terraform plan -out=tfplan

    - name: Terraform Apply
      if: |
        (github.ref == 'refs/heads/main' && github.event_name == 'push') ||
        (github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply')
      working-directory: ./envs/prod
      env:
        TF_VAR_postgres_admin_password: ${{ secrets.POSTGRES_ADMIN_PASSWORD }}
      run: terraform apply -auto-approve tfplan

    - name: Terraform Destroy
      if: github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'destroy'
      working-directory: ./envs/prod
      env:
        TF_VAR_postgres_admin_password: ${{ secrets.POSTGRES_ADMIN_PASSWORD }}
      run: terraform destroy -auto-approve

    - name: Output Terraform Values
      if: |
        (github.ref == 'refs/heads/main' && github.event_name == 'push') ||
        (github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply')
      working-directory: ./envs/prod
      run: terraform output -json > prod_outputs.json

    - name: Upload Terraform Outputs
      if: |
        (github.ref == 'refs/heads/main' && github.event_name == 'push') ||
        (github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply')
      uses: actions/upload-artifact@v4
      with:
        name: prod-terraform-outputs
        path: ./envs/prod/prod_outputs.json
